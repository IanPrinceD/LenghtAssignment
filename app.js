var characterCount = prompt ("Type anything you want. (please limit it into 150 characters)");
    alert ("You have written " + characterCount.length +" characters" +"you only have " + (150-characterCount.length) + " remaining characters");
